# my_xmrig_proxy

```
#!/bin/bash
sudo apt-get update
sudo wget -P /root --no-check-certificate https://raw.githubusercontent.com/KANIKIG/my_xmrig_proxy/main/setup_proxy.sh
sudo bash /root/setup_proxy.sh
```

